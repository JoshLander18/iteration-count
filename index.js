    // initialize the count as 0
    var i = 0;

    //when button is clicked function
    function increment() {
    
    //increment the count variable when the button i clicked
    i++;

    //change the count-el in the HTML to reflect the count
    document.getElementById("count-el").innerHTML = i;
}
